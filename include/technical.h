#pragma once
#include <vector>

using std::vector;

namespace Technical
{
    /**
     * @brief Calculate the standard deviation of a number array.
     */
    double standardDeviation(vector<double> input);

    /**
     * @brief Calculate the mean of a number array.
     */
    double mean(vector<double> input);

    /**
     * @brief Calculate the sum of a number array.
     */
    double sum(vector<double> input);
}